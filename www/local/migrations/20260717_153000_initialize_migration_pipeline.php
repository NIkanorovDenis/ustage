<?php

declare(strict_types=1);

use Bitrix\Main\DB\Connection;

return static function (Connection $connection): void {
    // Baseline migration: the runner itself creates the migration registry.
};
